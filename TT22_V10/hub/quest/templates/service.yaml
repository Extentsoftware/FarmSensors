apiVersion: v1
kind: Service
metadata:
  name: {{ template "quest.fullname" . }}
  labels:
    app: {{ template "quest.name" . }}
    chart: {{ template "quest.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "quest.name" . }}
    release: {{ .Release.Name }}
